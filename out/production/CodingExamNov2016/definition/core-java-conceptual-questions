Please answer the below core-java Questions:


What jdk contains or composed of and define each components?
-Java developement Kit which contains :
1-compiler : use to compile java files. 2- interpretor : to interpret the java files. 3- Java disassembler to diassable java class.
4-Java head file genereator : Generates header files and source files. Using javah command we can access that components. 5- java Debugger is used to debug java files.
6-java documentation components. 7- Java Applet viewer is used to excecute the online programs using applet.

What is IDE?
-Stands for Integrated development Enviroment. An IDE normally consists of a source code editor, build automation tools and a debugger.
What are the IDEs available in the market to support java?
-Eclipse , IntelliJ-netBeans-JDeveloper

Explain the entire java life cycle.
-By using the code edittor we write the code, Then by Compiler we compile those codes into bytecode. Then load the files using Java virtual machine class loader, loading the files on memory
. Next: bytecode vertifier verify the classes. All files are verified except for system classes.
 Interpreter:  decodes and executes bytecode for the Java virtual machine.

what is class?
Class is a blueprint for creating different objects which defines its properties and behaviors. Java class objects exhibit the properties and behaviors defined by its class. A class can contain fields and methods to describe the behavior of an object.
-
What is object?
Objects have states and behaviors and it can be an instance of the class

What is the entry point of an application?
-entry point is where control is transferred from the operating system to a computer program, at which place the processor enters a program or a code fragment and execution begins.

Why main is static?
- By being static it let to invoke via class.

What is the difference between an Interface and Abstract class?
-Methods of a Java interface are abstract and cannot have implementations. A Java abstract class can have instance methods that implements a default behavior.
and all the variables declared in a Java interface is by default final. An abstract class may contain non-final variables

Can you make an Object from an Interface and Abstract class ? if not how do you use it ?
-No we cant. By using the keyword implements and also implements all the methods declared from the interface

what is Access Specifier?
-By using public , private and other access modifier we decide whether that method /variable/constructors and etc are acceable from other classes or not.

What is OOP ? Define each feature of java OOP.
-Object Oriented Programing. Includes Object , class , Encapsulation , Inheritance , Polymorphism , Abstraction.
-Encapsulation : wraping code and data into a unit.
-Inheritence : let the user to create a class and inherit all the attribute and behaviour of the parent class.
-polymorphism: when we use overloading and overRiding
-Abstracyion : hiding the internal implementation from user.

What is API? Name list of API that you have used so far.
-Java Application Interface is all the classes that includes in jdk. Java.Util.* java.lang java.swing

Does java support multiple inheritance?
-No it doesnt support to avoid ambiguity , such as diamond.

What is method overloading and when it happens?
-Method Overloading is a feature that allows a class to have two or more methods having same name.

What is method overriding and when it happens?
-is that the class can give its own specific implementation to a inherited method without modifying the parent class

Explain exceptions in java and how to handle it.
-exception is an event, which occurs during the execution of a program, that disrupts the normal flow of the program's instructions
we can handle it by implementing try anc catch block

What is static keyword in java? How it has been used in variables and methods ?
-In Java, a static member belongs to the class itself, and if a variable or methods being declared as static you cant use non static variables for them. but vice versa of the this scnerio is correct.

What is final and how it has been used variables and methods?
-final means the value of the variable cant be changed after its declared final

What is final, finally and finalize?
-final is a keyword to restric the that method/variable from changing the value
-finally is a block that will execute after an exeption been thrown
-finalize method in java is a special method . finalize() is called before Garbage collector reclaim the Object, its last chance for any object to perform cleanup activity

What is a constructor ?
-Block of code that is used to create and object or instance of a class.

What is "this" keyword in java ?
-Is a refrence usually used inside a constructor or method to refer to the current object.

What is "super" keyword in java?
-refers to an immediate parents class object

What is JVM stand for ?
- Java virtual machine

What version of java are you using?
-The lates. 1.8

What is JAR stand for ?
Java archive

What is compile time and run time?
-compile time : syntax error or any other errirs that hapends during encoding to machine language.
-runtime such as runnning out of memory or wrong division and null, etc...
What is heap?
-The heap is the runtime data area from which memory for all class instances and arrays is allocated

How java manage it's memory?
-By using memory management tools such as stack, heap , segment-code.

What is the difference between String, StringBuffer and StringBuilder?
In java String are class/objects that can be used as variable.
StringBuffer and StringBuilder classes are used to modify or manipulate the string based on the desired script we are working on